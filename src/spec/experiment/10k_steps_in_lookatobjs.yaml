# @package _global_
defaults:
  - override /agent: constant
  - override /encoder: simclr

encoder:
  logging:
    save_interval: 1
env: MiniWorld-LookAtObjs-v0
training:
  num_processes: 8
  num_steps: 2000
  total_steps: 8e4 # 10k steps for each of 8 environments
